import React from "react";
import {SafeAreaView, View,} from "react-native";
import SilderApp from "./src/screen/Main"

export default App =(props)=>{
     return (
       <SafeAreaView style={{flex:1}}>
         <SilderApp/>
       </SafeAreaView>
     )
} 